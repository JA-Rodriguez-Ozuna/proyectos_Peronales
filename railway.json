{
  "build": {
    "buildCommand": "cd fronted-vo && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "python app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}